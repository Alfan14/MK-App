{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app.py" }
  ],
  "postinstall": "apt-get update && apt-get install -y libmysqlclient-dev"
}
